# 開發任務細分清單 (tasks.md) - 最終專業版

本文件將 `plan.md` 的里程碑拆解為具體的可執行任務，並設定檢查點 (Checkpoints)。

---

## 階段二：框架與基礎建設 (進行中)

### Task 2.1: 系統基礎配置 (日誌、路徑與異常捕獲)
- [x] **任務內容**：實作 `logging` 配置、跨平台路徑管理工具，以及全域異常捕獲機制 (sys.excepthook)。
- [x] **檢查點**：
    - [x] 執行 `python utils/paths.py` 能印出正確路徑。
    - [x] 故意引發一個錯誤，確認 `app.log` 能記錄到錯誤堆疊資訊。

### Task 2.2: 使用者偏好管理 (`ConfigManager`)
- [x] **任務內容**：建立 `utils/config_manager.py`，使用 JSON 儲存使用者設定。
- [x] **檢查點**：
    - [x] 執行 `python utils/config_manager.py` 驗證存取與損壞修復功能。

### Task 2.3: 資料庫版本遷移 (Migration)
- [x] **任務內容**：在 `db_manager.py` 加入版本檢查邏輯與 `db_info` 表。
- [x] **檢查點**：
    - [x] 驗證：執行初始化後，資料庫內出現版本號 1。

### Task 2.4: 主視窗導航框架與資產加載
- [x] **任務內容**：實作 `app.py` 基礎 UI 與 `AssetLoader` (處理圖示)。
- [x] **檢查點**：
    - [x] UI 驗證：點擊側邊欄能流暢切換不同的視圖。

### Task 2.5: 單字列表視圖 (List View) - 查詢
- [x] **任務內容**：實作 `views/list_view.py` 的分頁顯示與即時搜尋。
- [x] **檢查點**：
    - [x] 性能驗證：在 3000 單字下搜尋載入無明顯延遲 (實作防抖動與異步執行緒)。
    - [x] 功能驗證：支援大小寫不敏感、自動去空格搜尋。
    - [x] 優化：加入「僅顯示錯題」篩選開關。

### Task 2.6: 單字管理功能 (CRUD UI)
- [x] **任務內容**：實作單字的新增、編輯與刪除彈窗。
- [x] **檢查點**：
    - [x] 驗證：CRUD 操作後列表即時更新。

### Task 2.7: 程式啟動入口 (main.py)
- [x] **任務內容**：實作 `main.py` 整合啟動邏輯。
- [x] **檢查點**：
    - [x] 執行 `python main.py` 能成功啟動完整框架。

---

## 階段三：學習與發音系統

### Task 3.1: 異步音訊管理員 (`AudioManager`)
- [x] **任務內容**：實作單例模式音訊管理員，含異步下載與離線處理。
- [x] **檢查點**：
    - [x] 壓力測試：快速點擊不混音、不凍結 UI。

### Task 3.2: 單字閃卡介面 (Flashcards)
- [x] **任務內容**：開發閃卡視圖，綁定鍵盤快速鍵。
- [x] **檢查點**：
    - [x] 鍵盤操作能更新掌握度數據。
    - [x] 介面優化：提供實體按鈕與「結束練習」功能。

### Task 3.3: 設定頁面與快取管理
- [x] **任務內容**：實作設定介面，提供主題切換、清理快取。

---

## 階段四：測驗模式與品質保證

### Task 4.1: 測驗邏輯與掌握度演算法
- [x] **任務內容**：實作測驗模式與自動更新邏輯。
- [x] **檢查點**：
    - [x] 功能優化：實作「結算報告」與「錯題回顧」功能。

### Task 4.2: 全域單元測試
- [x] **任務內容**：在 `tests/` 執行所有邏輯測試。
- [x] **檢查點**：
    - [x] `test_db.py` 與 `test_config.py` 全部通過。

---

## 階段五：優化與發佈

### Task 5.1: 資料備份與匯出
- [x] **任務內容**：實作 SQLite 備份與 JSON/CSV 匯出。

### Task 5.2: 執行檔打包 (PyInstaller)
- [x] **任務內容**：實作 `build.py` 自動化打包腳本。
- [x] **檢查點**：
    - [x] 已配置資產嵌入 (assets, vocabulary.db) 與 CustomTkinter 相容性。

